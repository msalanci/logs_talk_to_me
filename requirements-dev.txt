# Driver-machine Python dependencies for deploying and running LTTM.
#
# Installed in the root-level .venv and used by:
#   - `agentcore launch` (runtime deploy from laptop)
#   - ad-hoc helper scripts
#   - alexandra.sh's Cognito token refresh path
#
# NOT the same file as agents/requirements.txt.
# agents/requirements.txt lists deps installed INSIDE the AgentCore runtime
# container during `agentcore launch`. Do not merge the two.

bedrock-agentcore-starter-toolkit>=0.3,<1.0
boto3>=1.34,<2.0
